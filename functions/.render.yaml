services:
  - type: web
    name: whatsapp-bot
    runtime: docker
    buildCommand: docker build -t whatsapp-bot .
    startCommand: docker run -p 3000:3000 whatsapp-bot
    envVars:
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/chromium
      - key: DISPLAY
        value: :99
